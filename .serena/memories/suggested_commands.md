# Suggested Commands
- `uv run python src/run_server.py`: Start the backend server.
- `mise run dev`: Start the full stack (Tauri + Vite + Python sidecar).
- `uv sync`: Update dependencies.
- `git log --oneline`: Check commit history.
- `uv pip list`: Check installed packages for LSP.

